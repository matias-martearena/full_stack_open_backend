{
  "name": "part3",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "type": "module",
  "scripts": {
    "start": "node app.js",
    "dev": "node --watch app.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf dist && cd ../full_stack_open/part2/2.16 - 2.17 The Phonebook && npm run build && cp -r dist ../../../full_stack_open_backend",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push -u origin main && npm start"
  },
  "author": "Matias Martearena",
  "license": "ISC",
  "dependencies": {
    "cors": "2.8.5",
    "dotenv": "16.4.5",
    "express": "4.19.2",
    "mongoose": "8.4.0",
    "morgan": "1.10.0"
  },
  "devDependencies": {
    "standard": "^17.1.0"
  },
  "eslintConfig": {
    "extends": "standard"
  }
}